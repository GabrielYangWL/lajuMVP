import type { Metadata } from "next";

export const metadata: Metadata = { title: "Check-In" };

export default function CheckInPage() {
  return (
    <section>
      <h1 className="text-2xl font-bold mb-6">Check-In Harian</h1>
      {/* CheckInList + StartCheckIn CTA */}
    </section>
  );
}
