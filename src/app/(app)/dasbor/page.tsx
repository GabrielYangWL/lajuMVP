import type { Metadata } from "next";

export const metadata: Metadata = { title: "Dasbor" };

export default function DasborPage() {
  return (
    <section>
      <h1 className="text-2xl font-bold mb-6">Selamat datang di LAJU</h1>
      {/* DasborSummary, active Mimpi card, check-in prompt */}
    </section>
  );
}
