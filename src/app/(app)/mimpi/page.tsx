import type { Metadata } from "next";

export const metadata: Metadata = { title: "Mimpi Saya" };

export default function MimpiPage() {
  return (
    <section>
      <h1 className="text-2xl font-bold mb-6">Mimpi Saya</h1>
      {/* MimpiList + CreateMimpi CTA */}
    </section>
  );
}
