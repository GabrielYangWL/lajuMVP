export const contentService = {
  async list(_cursor?: string, _limit?: number) {
    // TODO: contentRepository.findPublished + buildPaginatedResponse
    throw new Error("Not implemented");
  },
};
